# ados-vocabulary/ — MANIFEST

ADOS vocabulary mapping reference: how this project's terms map onto the shared
ADOS vocabulary (archetypes, domains, coordinates, governance surfaces).

## How an agent EXECUTES this folder
- Not executed. Reference material consulted when reconciling project terms with
  ADOS naming.

## How an agent CHANGES this folder
- Keep it consistent with `ontology/`, `geometry/`, and `schema/ados/`; it
  documents the mapping, it does not define authority. Update it when an
  ontology/geometry term is renamed.

## Files
- `ADOS-MAPPING.md` — the term-to-ADOS mapping notes.
